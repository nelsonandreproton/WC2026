[project]
name = "wc2026bot"
version = "0.1.0"
description = "Telegram bot for FIFA World Cup 2026 score predictions"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
